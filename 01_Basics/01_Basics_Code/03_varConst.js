// TIPS
// constants are defined using keyword 'const'
// 'var' is also used to define variables similar to 'let'. But 'var' is used for global variables 

var score = 100
score = 130 // updated score

const bonus = 50 // defining a constant variable 'bonus'

var complex = ((4 + 4) * 5) / 2 // mathematical operations accoring to BODMAS rule

var finalScore = (score + bonus) * 1.8

console.log("finalScore: " + finalScore)
console.log("complex: " + complex)

let tempInFarhenheite = 100

let tempOutCelsius = (tempInFarhenheite - 32) * 5 / 9

console.log("Temperature in celsius: " + tempOutCelsius)